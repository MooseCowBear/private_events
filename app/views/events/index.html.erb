<h1>Events</h1>

<% if @events.empty? %>
  <%= "There are no events scheduled." %>
<% end %>

  <ul class="events">
    <% @events.each do |event| %>
      <li>
        <%= link_to event.name, event %>
        <%= event.event_date %>
        <%= event.location %>
      </li>
    <% end %>
  </ul>